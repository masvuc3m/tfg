pipeline {
    agent any

    environment {
        registry = 'masvuc3m/tfg'
        registryCredential = 'docker-credentials'
        dockerImage = ''
        GKE_CREDENTIALS = credentials('gke-credentials')
    }

    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }

        stage('SonarQube analysis') {
            steps {
                mvn clean verify sonar:sonar \
                -Dsonar.projectKey=tfg \
                -Dsonar.projectName='tfg' \
                -Dsonar.host.url=http://10.204.0.2:9000 \
                -Dsonar.token=sqp_1f5374a1009cbd418715e9ebbcd6389097ada52d
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    dockerImage = docker.build registry + ":$BUILD_NUMBER"
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry( '', registryCredential) {
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Deploy to GKE') {
            steps {
                script {
                    withCredentials([file(credentialsId: 'gke-credentials', variable: 'KUBECONFIG')]) {
                        sh 'kubectl apply -f k8s/deployment.yaml'
                    }
                }
            }
        }

        stage('Cleaning up') {
            steps{
                sh "docker rmi $registry:$BUILD_NUMBER"
            }
        }
    }
}
